# Auth token (required for WebSocket and API). Leave empty to disable auth.
AUTH_TOKEN=your-secret-token

# Server
HOST=0.0.0.0
PORT=8000

# WebSocket paths (optional)
WS_CLIENT_PATH=/ws/client
WS_MONITOR_PATH=/ws/monitor
