# Backend WebSocket URL (no trailing slash). Example: ws://localhost:8000/kiosk-api/ws/client
BACKEND_WS_URL=ws://localhost:8000/kiosk-api/ws/client
# Auth token (must match backend AUTH_TOKEN)
AUTH_TOKEN=your-secret-token
# Optional: group to join on connect
GROUP_ID=
